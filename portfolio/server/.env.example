# Port du serveur backend
PORT=5000

# Configuration email pour le formulaire de contact (optionnel)
# Laisser vide pour désactiver l'envoi réel (les messages seront seulement loggés/stockés)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
CONTACT_RECEIVER_EMAIL=
